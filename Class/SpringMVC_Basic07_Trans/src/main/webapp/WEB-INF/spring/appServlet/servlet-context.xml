<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.3.xsd">

	<!-- 모든 컨트롤러가 공통 사용 DAO 빈 객체 참조 -->
	<context:annotation-config />
	<context:component-scan
		base-package="ncontroller" />
	<context:component-scan base-package="dao" />
	<context:component-scan base-package="service" />
	
	<!-- Transaction 처리하기 -->
	<!-- 
		Legacy 방식: IOC 여러 개 = Root Container 하나 & 하위 Container 여러 개
			-> 문제점: 하위 Container 끼리는 참조 불가
			-> 장점: Root Container는 하위 Container에서 참조 가능
			
		tx 사용 이유: 참조되는 Bean이 현재 Container 안에 있기 때문
	 -->
	 
	 <!-- Transaction 만들기 -->
	<bean id="transactionManager"  class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="driverManagerDataSource" /> 
	</bean>
	<tx:annotation-driven transaction-manager="transactionManager"/> 
	<!-- 
		만약 @Transactional Annotation을 사용한다면, tx:annotation-driven(위의 한 줄) 필요하다.	
	 -->
	
	<!-- base-package="ncontroller" 패키지 안에 모든 클래스를 스캔합니다 클래스 위에 @Controller 
		@Service 의미 @Repository 의미 @Component 의미 @RestController bean 으로 만들어야지 ... 
		<bean id="/customer/notice.htm" class="controllers.customer.NoticeController"></bean> 
		작업이 필요없다 -->


	<!-- 파일 업로드 보조(반드시) -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="maxUploadSize" value="10485760"></property>
		<property name="defaultEncoding" value="UTF-8"></property>
	</bean>

</beans>


